{
  "version": 2,
  "buildCommand": "cd frontend && npm install && npx convex deploy --cmd 'npm run build'",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
