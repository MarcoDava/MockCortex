services:
  - type: web
    name: mockcortex-backend
    env: node
    rootDir: backend
    buildCommand: apt-get install -y ffmpeg && npm install
    startCommand: node src/index.js
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: ELEVENLABS_API_KEY
        sync: false
      - key: PORT
        value: 10000
